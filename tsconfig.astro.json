{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    /* Modules */
    "moduleResolution": "bundler",

    /* Emit */
    "noEmit": true,

    /* Language and Environment */
    "jsx": "preserve"
  }
}
